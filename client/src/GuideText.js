// prompts the user to select a category(click one of the buttons)
const GuideText = () => {
  return (
    <div class="has-text-centered">
      <h2 class="is-size-4">Choose a category.</h2>
    </div>
  );
};

export default GuideText;
