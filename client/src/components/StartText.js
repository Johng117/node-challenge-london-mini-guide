// Initial text that asks the user to select an option on the select box
const StartText = () => {
  return (
    <div class="has-text-centered">
      <h2 class="is-size-4">Choose a district.</h2>
    </div>
  );
};

export default StartText;
